# Celery Worker

Optional Python sidecar for tasks that fit the Python ecosystem better than the main Node.js worker.

Useful workloads:

- data enrichment
- ETL
- document ingestion
- embeddings preprocessing
- offline AI preprocessing

Uses Redis as broker and result backend.
